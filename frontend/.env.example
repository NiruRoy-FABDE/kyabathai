# Frontend env — set this in Netlify (Site settings → Environment variables).
# It is the public URL of your Python backend on Render. NO trailing slash.
VITE_API_BASE=https://kyabathai-api.onrender.com
